#set document(title: [Scheme–ClojureScript output comparison])
#set page(margin: 22mm)
#set text(size: 10pt)

= Scheme–ClojureScript output comparison

This report is generated by #raw(lang: "sh", "node scripts/compare-scm-cljs-outputs.mjs"). It compares captured ClojureScript/Emmy results with the historical scmutils result comments next to the same source blocks. Printer aliases, exact ratios, commutative ordering, and algebraic rearrangements that agree at nine deterministic substitution points are treated as equivalent.

Compared #186 positions having outputs from both systems: #186 mechanically equivalent, #0 mathematically different, and #0 unresolved. The #29 missing comparison counterparts are excluded from these totals and listed separately below. Among the equivalent pairs, #0 have CLJS output at least 1.5 times and 120 characters longer than the Scheme output. Unresolved entries are not called differences because prose or truncated output cannot safely establish unequal mathematical content.

= Equivalent but disproportionately verbose

These pairs passed the mathematical checks, but the CLJS rendering is much longer. This usually means scmutils simplified automatically while Emmy needs an explicit #raw(lang: "clojure", "simplify") around the result-producing expression. They remain passing comparisons; this is a maintainability and typesetting warning.

None.
None found.

= Confirmed mathematical differences

None.
None found.

= Missing comparison counterparts

29 positions either have a result on only one side or only a qualitative Scheme placeholder. They are kept separate because they do not represent failed mathematical comparisons.

#table(
  columns: (1.25fr, 1.25fr, 4fr),
  inset: 6pt,
  stroke: rgb("#c7cdd4"),
  table.header(
    table.cell(fill: rgb("#dbeafe"))[*Block*],
    table.cell(fill: rgb("#dbeafe"))[*Status*],
    table.cell(fill: rgb("#dbeafe"))[*Best diagnosis*],
  ),
  table.cell(fill: rgb("#dbeafe"))[#raw("prologue-004:1")],
[#raw("No Scheme reference")],
[#raw("The CLJS runner captures a value for which the book stores no historical scmutils output. This needs an oracle or manual review, not textual comparison.")],
table.cell(fill: rgb("#dbeafe"))[#raw("prologue-005:1")],
[#raw("No Scheme reference")],
[#raw("The CLJS runner captures a value for which the book stores no historical scmutils output. This needs an oracle or manual review, not textual comparison.")],
table.cell(fill: rgb("#dbeafe"))[#raw("chapter002-004:1")],
[#raw("No CLJS capture")],
[#raw("The Scheme block contains apparent output but CLJS has no corresponding capture. Best guess: the form is deliberately non-executable, was classified as setup/definition, or still needs per-form capture support.")],
table.cell(fill: rgb("#dbeafe"))[#raw("chapter003-010:1")],
[#raw("No CLJS capture")],
[#raw("The Scheme block contains apparent output but CLJS has no corresponding capture. Best guess: the form is deliberately non-executable, was classified as setup/definition, or still needs per-form capture support.")],
table.cell(fill: rgb("#dbeafe"))[#raw("chapter007-011:1")],
[#raw("No CLJS capture")],
[#raw("The Scheme block contains apparent output but CLJS has no corresponding capture. Best guess: the form is deliberately non-executable, was classified as setup/definition, or still needs per-form capture support.")],
table.cell(fill: rgb("#dbeafe"))[#raw("chapter007-037:1")],
[#raw("No Scheme reference")],
[#raw("The CLJS runner captures a value for which the book stores no historical scmutils output. This needs an oracle or manual review, not textual comparison.")],
table.cell(fill: rgb("#dbeafe"))[#raw("chapter007-039:1")],
[#raw("No Scheme reference")],
[#raw("The CLJS runner captures a value for which the book stores no historical scmutils output. This needs an oracle or manual review, not textual comparison.")],
table.cell(fill: rgb("#dbeafe"))[#raw("chapter008-013:1")],
[#raw("Qualitative Scheme placeholder")],
[#raw("The Scheme side records only a qualitative placeholder such as ‘mess’, so there is no mathematical datum to compare.")],
table.cell(fill: rgb("#dbeafe"))[#raw("chapter008-014:1")],
[#raw("Qualitative Scheme placeholder")],
[#raw("The Scheme side records only a qualitative placeholder such as ‘mess’, so there is no mathematical datum to compare.")],
table.cell(fill: rgb("#dbeafe"))[#raw("chapter008-017:1")],
[#raw("No Scheme reference")],
[#raw("The CLJS runner captures a value for which the book stores no historical scmutils output. This needs an oracle or manual review, not textual comparison.")],
table.cell(fill: rgb("#dbeafe"))[#raw("chapter008-019:1")],
[#raw("Printed values not returned")],
[#raw("The inner expression is now simplified and prints four zeros, matching Scheme, but the smoke runner captures only the outer for-each return value nil. Treat this as a capture-shape gap, not a mathematical mismatch.")],
table.cell(fill: rgb("#dbeafe"))[#raw("chapter008-019:2")],
[#raw("No CLJS capture")],
[#raw("The Scheme block contains apparent output but CLJS has no corresponding capture. Best guess: the form is deliberately non-executable, was classified as setup/definition, or still needs per-form capture support.")],
table.cell(fill: rgb("#dbeafe"))[#raw("chapter008-019:3")],
[#raw("No CLJS capture")],
[#raw("The Scheme block contains apparent output but CLJS has no corresponding capture. Best guess: the form is deliberately non-executable, was classified as setup/definition, or still needs per-form capture support.")],
table.cell(fill: rgb("#dbeafe"))[#raw("chapter008-019:4")],
[#raw("No CLJS capture")],
[#raw("The Scheme block contains apparent output but CLJS has no corresponding capture. Best guess: the form is deliberately non-executable, was classified as setup/definition, or still needs per-form capture support.")],
table.cell(fill: rgb("#dbeafe"))[#raw("chapter008-035:1")],
[#raw("No CLJS capture")],
[#raw("The Scheme block contains apparent output but CLJS has no corresponding capture. Best guess: the form is deliberately non-executable, was classified as setup/definition, or still needs per-form capture support.")],
table.cell(fill: rgb("#dbeafe"))[#raw("chapter008-037:1")],
[#raw("No CLJS capture")],
[#raw("The Scheme block contains apparent output but CLJS has no corresponding capture. Best guess: the form is deliberately non-executable, was classified as setup/definition, or still needs per-form capture support.")],
table.cell(fill: rgb("#dbeafe"))[#raw("chapter008-038:1")],
[#raw("No CLJS capture")],
[#raw("The Scheme block contains apparent output but CLJS has no corresponding capture. Best guess: the form is deliberately non-executable, was classified as setup/definition, or still needs per-form capture support.")],
table.cell(fill: rgb("#dbeafe"))[#raw("chapter009-019:1")],
[#raw("Qualitative Scheme placeholder")],
[#raw("The Scheme side records only a qualitative placeholder such as ‘mess’, so there is no mathematical datum to compare.")],
table.cell(fill: rgb("#dbeafe"))[#raw("chapter009-020:1")],
[#raw("No Scheme reference")],
[#raw("The CLJS runner captures a value for which the book stores no historical scmutils output. This needs an oracle or manual review, not textual comparison.")],
table.cell(fill: rgb("#dbeafe"))[#raw("chapter009-029:1")],
[#raw("No Scheme reference")],
[#raw("The CLJS runner captures a value for which the book stores no historical scmutils output. This needs an oracle or manual review, not textual comparison.")],
table.cell(fill: rgb("#dbeafe"))[#raw("appendix_a-003:1")],
[#raw("No Scheme reference")],
[#raw("The CLJS runner captures a value for which the book stores no historical scmutils output. This needs an oracle or manual review, not textual comparison.")],
table.cell(fill: rgb("#dbeafe"))[#raw("appendix_b-019:2")],
[#raw("No CLJS capture")],
[#raw("The ports expose different numbers of result-producing forms. Best guess: block merging or definition/output classification changed the alignment; inspect the forms individually before comparing by position.")],
table.cell(fill: rgb("#dbeafe"))[#raw("appendix_b-023:1")],
[#raw("No Scheme reference")],
[#raw("The CLJS runner captures a value for which the book stores no historical scmutils output. This needs an oracle or manual review, not textual comparison.")],
table.cell(fill: rgb("#dbeafe"))[#raw("appendix_c-005:1")],
[#raw("Qualitative Scheme placeholder")],
[#raw("The Scheme side records only a qualitative placeholder such as ‘mess’, so there is no mathematical datum to compare.")],
table.cell(fill: rgb("#dbeafe"))[#raw("errata-024:1")],
[#raw("No CLJS capture")],
[#raw("These are explanatory errata comments, not evaluator results; the block is deliberately non-executable.")],
table.cell(fill: rgb("#dbeafe"))[#raw("errata-024:2")],
[#raw("No CLJS capture")],
[#raw("These are explanatory errata comments, not evaluator results; the block is deliberately non-executable.")],
table.cell(fill: rgb("#dbeafe"))[#raw("errata-024:3")],
[#raw("No CLJS capture")],
[#raw("These are explanatory errata comments, not evaluator results; the block is deliberately non-executable.")],
table.cell(fill: rgb("#dbeafe"))[#raw("errata-024:4")],
[#raw("No CLJS capture")],
[#raw("These are explanatory errata comments, not evaluator results; the block is deliberately non-executable.")],
table.cell(fill: rgb("#dbeafe"))[#raw("errata-024:5")],
[#raw("No CLJS capture")],
[#raw("These are explanatory errata comments, not evaluator results; the block is deliberately non-executable.")],
)

= Other unresolved comparisons

0 positions have results on both sides but cannot yet be compared safely.

None.

